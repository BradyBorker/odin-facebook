<!-- <turbo-frame id="post-form" src=<%= new_post_path %>></turbo-frame>-->
<%= render "new_post" %>

<h1>Posts:</h1>
<div class="posts-container">
    <% @posts.each do |post| %>
        <%= render "post", post: post %>
        <%= render "comments/new_comment", comment: @comment, post_id: post.id %>
    <% end %>
</div>